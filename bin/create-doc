#!/usr/bin/env zsh
# Uses new signature in which 'PAPER_DIR' environment variable must be defined
cd "$PAPER_DIR"

source paper-components/defs.zsh

paper-components/bin/rev-text | text-pipeline-docx -o "build/draft.docx"
